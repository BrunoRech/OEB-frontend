import styled from 'styled-components';
import { Form, Button as button } from 'semantic-ui-react';

export const CitySelect = styled(Form.Select)`
width: 82%;
`;

export const Button = styled(button)`
    min-width:100px;
`;

